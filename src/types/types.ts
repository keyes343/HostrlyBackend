import {categoryFood as food} from './enums';

export type menu = {
    date?:Date, dateNum:number,
    Rice: string[],
    Dal: string[],
    Sabji: string[],
    Fruits: string[],
    Meat: string[],
    Others: string[]
}

